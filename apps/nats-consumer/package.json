{
  "name": "nats-consumer1",
  "version": "1.0.0",
  "description": "nats consumer",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "dev": "ts-node-dev --no-notify --transpile-only --respawn -- src/index.ts",
    "start": "NODE_ENV=production node dist/index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "nats": "^2.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.9.2",
    "rimraf": "^5.0.5",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  }
}
